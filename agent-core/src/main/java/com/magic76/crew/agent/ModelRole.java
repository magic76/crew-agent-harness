package com.magic76.crew.agent;

/** High-level model responsibilities shared across Crew products. */
public enum ModelRole {
    REALTIME_CONVERSATION,
    REASONING,
    GENERATION,
    SELF_IMPROVEMENT
}
